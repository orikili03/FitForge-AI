# Leave empty to use Vite dev proxy (requests to /auth, /workouts etc. go to http://localhost:4000).
# For production or if proxy fails, set to your backend URL, e.g. http://localhost:4000
VITE_API_BASE_URL=


