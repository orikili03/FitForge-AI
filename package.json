{
  "private": true,
  "scripts": {
    "build": "npm run build --prefix backend",
    "start": "npm start --prefix backend"
  },
  "dependencies": {
    "wodlab-backend": "file:backend",
    "wodlab-frontend": "file:frontend"
  }
}
